# MongoDB Atlas Configuration
# Get your connection string from https://cloud.mongodb.com/
# Replace the placeholders with your actual values
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority&appName=YOUR_APP_NAME
DB_NAME=freelancer-marketplace

# JWT Configuration
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=3002
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Replace YOUR_USERNAME, YOUR_PASSWORD, YOUR_CLUSTER, YOUR_DATABASE, YOUR_APP_NAME with your MongoDB Atlas details
# 3. Generate a secure JWT_SECRET for production use
# 4. Save the file and restart the server
